# 词韵系统数据问题报告

## 问题清单

### 问题1：代表作数量严重不足 ⚠️

**现状**：
- 无代表作的词牌：61个（共100个词牌）
- 正体少于5首的词牌：35个
- 只有1个词牌正体有5首代表作

**用户期望**：
- 正体格律：5首代表作
- 每个变体格律：1首代表作

**影响**：
- 词牌大全页面点击"代表作"按钮可能无数据显示
- 填词界面可能无参考代表作

---

### 问题2：格律与代表作关联不匹配 ⚠️

**现状**：72个词牌的格律数量与代表作key数量不匹配

**示例**：
| 词牌 | 格律数量 | 代表作key数量 | 问题 |
|------|----------|---------------|------|
| 沁园春 | 7种 | 0个 | 无代表作 |
| 摸鱼儿 | 9种 | 0个 | 无代表作 |
| 八声甘州 | 7种 | 0个 | 无代表作 |
| 渔家傲 | 4种 | 0个 | 无代表作 |
| 念奴娇 | 1种 | 2个 | key可能是'0','1'等 |
| 鹧鸪天 | 2种 | 3个 | key数量超过格律数 |

**根因**：
- `representative_works.json` 中使用 `0`, `1`, `2`... 作为key
- 这些key应该对应 `patterns` 数组的索引
- 但部分词牌的代表作key与格律数组长度不匹配

---

### 问题3：代表作文本格式问题

**现状**：部分代表作包含空行

**示例**：ID 18（醉花阴）代表作有空行

**期望**：
- 按韵脚换行（句号、感叹号、问号结尾的句子）
- 逗号不换行
- 无多余空行
- 无连续空格

---

## 数据结构说明

### 格律数据结构（cipai_data.py）

```python
CIPAI_DATABASE = [
    {
        "id": 1,
        "name": "水调歌头",
        "patterns": [  # 数组，每个元素是一个格律
            {
                "name": "正体（毛滂体）",
                "total_chars": 95,
                "sentences": [...],
                ...
            },
            {
                "name": "变体一（周紫芝体）",
                ...
            },
            ...
        ]
    },
    ...
]
```

### 代表作数据结构（representative_works.json）

```json
{
    "1": {  // 词牌ID
        "0": [  // 格律索引（对应patterns[0]）
            {
                "author": "毛滂",
                "title": "元会曲·九金增宋重",
                "dynasty": "宋",
                "text": "九金增宋重，八玉变秦馀..."
            }
        ],
        "1": [  // 格律索引（对应patterns[1]）
            {...}
        ],
        ...
    },
    ...
}
```

---

## 修复方案

### 第一优先级：补全代表作数据

**工作量**：需要为61个无代表作的词牌补充数据

**数据来源建议**：
1. 百度百科/古诗文网等权威来源
2. 参考 `cipai_data.py` 中格律的 `name` 字段（通常包含代表作名称）
3. 例如 "正体（苏轼《水调歌头·明月几时有》体）" 可提取代表作信息

### 第二优先级：修正格律关联

**步骤**：
1. 检查每个词牌的 `patterns` 数组长度
2. 确保 `representative_works.json` 中的key `0`, `1`, `2`... 与 patterns 索引对应
3. 删除多余的key，补充缺失的key

### 第三优先级：清理文本格式

**步骤**：
1. 移除代表作文本中的连续空格
2. 移除连续换行符
3. 统一标点符号格式

---

## 当前系统能正常工作的部分

1. ✅ 格律选择器（`pattern_count` 已修复）
2. ✅ 填词格子展示（`tone`/`chars` 一致性已修复）
3. ✅ 有代表作的词牌可正常展示

## 需要立即修复的问题

1. ❌ 61个词牌无代表作数据
2. ❌ 72个词牌格律与代表作关联不匹配
